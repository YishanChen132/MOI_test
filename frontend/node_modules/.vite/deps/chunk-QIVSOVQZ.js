import {
  init_esm
} from "./chunk-HHD3ABUE.js";
import {
  _defineProperty,
  init_defineProperty
} from "./chunk-H5Z4CSWQ.js";
import {
  __commonJS,
  __esm,
  __export,
  __toESM
} from "./chunk-PLDDJCW6.js";

// ../node_modules/@loaders.gl/images/dist/esm/lib/utils/version.js
var VERSION;
var init_version = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/utils/version.js"() {
    VERSION = true ? "3.4.15" : "latest";
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/env-utils/assert.js
function assert(condition, message) {
  if (!condition) {
    throw new Error(message || "loader assertion failed.");
  }
}
var init_assert = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/env-utils/assert.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/env-utils/globals.js
var globals, self_, window_, global_, document_, isBrowser, matches, nodeVersion;
var init_globals = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/env-utils/globals.js"() {
    globals = {
      self: typeof self !== "undefined" && self,
      window: typeof window !== "undefined" && window,
      global: typeof global !== "undefined" && global,
      document: typeof document !== "undefined" && document
    };
    self_ = globals.self || globals.window || globals.global || {};
    window_ = globals.window || globals.self || globals.global || {};
    global_ = globals.global || globals.self || globals.window || {};
    document_ = globals.document || {};
    isBrowser = Boolean(typeof process !== "object" || String(process) !== "[object process]" || process.browser);
    matches = typeof process !== "undefined" && process.version && /v([0-9]*)/.exec(process.version);
    nodeVersion = matches && parseFloat(matches[1]) || 0;
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/option-utils/merge-loader-options.js
var init_merge_loader_options = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/option-utils/merge-loader-options.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/version.js
var init_version2 = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/version.js"() {
    if (false) {
      console.error("loaders.gl: The __VERSION__ variable is not injected using babel plugin. Latest unstable workers would be fetched from the CDN.");
    }
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/assert.js
function assert2(condition, message) {
  if (!condition) {
    throw new Error(message || "loaders.gl assertion failed.");
  }
}
var init_assert2 = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/assert.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/globals.js
var globals2, self_2, window_2, global_2, document_2, isBrowser2, isMobile, matches2, nodeVersion2;
var init_globals2 = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/env-utils/globals.js"() {
    globals2 = {
      self: typeof self !== "undefined" && self,
      window: typeof window !== "undefined" && window,
      global: typeof global !== "undefined" && global,
      document: typeof document !== "undefined" && document
    };
    self_2 = globals2.self || globals2.window || globals2.global || {};
    window_2 = globals2.window || globals2.self || globals2.global || {};
    global_2 = globals2.global || globals2.self || globals2.window || {};
    document_2 = globals2.document || {};
    isBrowser2 = typeof process !== "object" || String(process) !== "[object process]" || process.browser;
    isMobile = typeof window !== "undefined" && typeof window.orientation !== "undefined";
    matches2 = typeof process !== "undefined" && process.version && /v([0-9]*)/.exec(process.version);
    nodeVersion2 = matches2 && parseFloat(matches2[1]) || 0;
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-job.js
var WorkerJob;
var init_worker_job = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-job.js"() {
    init_defineProperty();
    init_assert2();
    WorkerJob = class {
      constructor(jobName, workerThread) {
        _defineProperty(this, "name", void 0);
        _defineProperty(this, "workerThread", void 0);
        _defineProperty(this, "isRunning", true);
        _defineProperty(this, "result", void 0);
        _defineProperty(this, "_resolve", () => {
        });
        _defineProperty(this, "_reject", () => {
        });
        this.name = jobName;
        this.workerThread = workerThread;
        this.result = new Promise((resolve, reject) => {
          this._resolve = resolve;
          this._reject = reject;
        });
      }
      postMessage(type, payload) {
        this.workerThread.postMessage({
          source: "loaders.gl",
          type,
          payload
        });
      }
      done(value) {
        assert2(this.isRunning);
        this.isRunning = false;
        this._resolve(value);
      }
      error(error) {
        assert2(this.isRunning);
        this.isRunning = false;
        this._reject(error);
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/node/worker_threads-browser.js
var Worker2;
var init_worker_threads_browser = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/node/worker_threads-browser.js"() {
    Worker2 = class {
      terminate() {
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-utils/get-loadable-worker-url.js
function getLoadableWorkerURL(props) {
  assert2(props.source && !props.url || !props.source && props.url);
  let workerURL = workerURLCache.get(props.source || props.url);
  if (!workerURL) {
    if (props.url) {
      workerURL = getLoadableWorkerURLFromURL(props.url);
      workerURLCache.set(props.url, workerURL);
    }
    if (props.source) {
      workerURL = getLoadableWorkerURLFromSource(props.source);
      workerURLCache.set(props.source, workerURL);
    }
  }
  assert2(workerURL);
  return workerURL;
}
function getLoadableWorkerURLFromURL(url) {
  if (!url.startsWith("http")) {
    return url;
  }
  const workerSource = buildScriptSource(url);
  return getLoadableWorkerURLFromSource(workerSource);
}
function getLoadableWorkerURLFromSource(workerSource) {
  const blob = new Blob([workerSource], {
    type: "application/javascript"
  });
  return URL.createObjectURL(blob);
}
function buildScriptSource(workerUrl) {
  return "try {\n  importScripts('".concat(workerUrl, "');\n} catch (error) {\n  console.error(error);\n  throw error;\n}");
}
var workerURLCache;
var init_get_loadable_worker_url = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-utils/get-loadable-worker-url.js"() {
    init_assert2();
    workerURLCache = /* @__PURE__ */ new Map();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-utils/get-transfer-list.js
function getTransferList(object) {
  let recursive = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
  let transfers = arguments.length > 2 ? arguments[2] : void 0;
  const transfersSet = transfers || /* @__PURE__ */ new Set();
  if (!object) {
  } else if (isTransferable(object)) {
    transfersSet.add(object);
  } else if (isTransferable(object.buffer)) {
    transfersSet.add(object.buffer);
  } else if (ArrayBuffer.isView(object)) {
  } else if (recursive && typeof object === "object") {
    for (const key in object) {
      getTransferList(object[key], recursive, transfersSet);
    }
  }
  return transfers === void 0 ? Array.from(transfersSet) : [];
}
function isTransferable(object) {
  if (!object) {
    return false;
  }
  if (object instanceof ArrayBuffer) {
    return true;
  }
  if (typeof MessagePort !== "undefined" && object instanceof MessagePort) {
    return true;
  }
  if (typeof ImageBitmap !== "undefined" && object instanceof ImageBitmap) {
    return true;
  }
  if (typeof OffscreenCanvas !== "undefined" && object instanceof OffscreenCanvas) {
    return true;
  }
  return false;
}
var init_get_transfer_list = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-utils/get-transfer-list.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-thread.js
var NOOP, WorkerThread;
var init_worker_thread = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-thread.js"() {
    init_defineProperty();
    init_worker_threads_browser();
    init_globals2();
    init_assert2();
    init_get_loadable_worker_url();
    init_get_transfer_list();
    NOOP = () => {
    };
    WorkerThread = class {
      static isSupported() {
        return typeof Worker !== "undefined" && isBrowser2 || typeof Worker2 !== "undefined" && !isBrowser2;
      }
      constructor(props) {
        _defineProperty(this, "name", void 0);
        _defineProperty(this, "source", void 0);
        _defineProperty(this, "url", void 0);
        _defineProperty(this, "terminated", false);
        _defineProperty(this, "worker", void 0);
        _defineProperty(this, "onMessage", void 0);
        _defineProperty(this, "onError", void 0);
        _defineProperty(this, "_loadableURL", "");
        const {
          name,
          source,
          url
        } = props;
        assert2(source || url);
        this.name = name;
        this.source = source;
        this.url = url;
        this.onMessage = NOOP;
        this.onError = (error) => console.log(error);
        this.worker = isBrowser2 ? this._createBrowserWorker() : this._createNodeWorker();
      }
      destroy() {
        this.onMessage = NOOP;
        this.onError = NOOP;
        this.worker.terminate();
        this.terminated = true;
      }
      get isRunning() {
        return Boolean(this.onMessage);
      }
      postMessage(data, transferList) {
        transferList = transferList || getTransferList(data);
        this.worker.postMessage(data, transferList);
      }
      _getErrorFromErrorEvent(event) {
        let message = "Failed to load ";
        message += "worker ".concat(this.name, " from ").concat(this.url, ". ");
        if (event.message) {
          message += "".concat(event.message, " in ");
        }
        if (event.lineno) {
          message += ":".concat(event.lineno, ":").concat(event.colno);
        }
        return new Error(message);
      }
      _createBrowserWorker() {
        this._loadableURL = getLoadableWorkerURL({
          source: this.source,
          url: this.url
        });
        const worker = new Worker(this._loadableURL, {
          name: this.name
        });
        worker.onmessage = (event) => {
          if (!event.data) {
            this.onError(new Error("No data received"));
          } else {
            this.onMessage(event.data);
          }
        };
        worker.onerror = (error) => {
          this.onError(this._getErrorFromErrorEvent(error));
          this.terminated = true;
        };
        worker.onmessageerror = (event) => console.error(event);
        return worker;
      }
      _createNodeWorker() {
        let worker;
        if (this.url) {
          const absolute = this.url.includes(":/") || this.url.startsWith("/");
          const url = absolute ? this.url : "./".concat(this.url);
          worker = new Worker2(url, {
            eval: false
          });
        } else if (this.source) {
          worker = new Worker2(this.source, {
            eval: true
          });
        } else {
          throw new Error("no worker");
        }
        worker.on("message", (data) => {
          this.onMessage(data);
        });
        worker.on("error", (error) => {
          this.onError(error);
        });
        worker.on("exit", (code) => {
        });
        return worker;
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-pool.js
var WorkerPool;
var init_worker_pool = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-pool.js"() {
    init_defineProperty();
    init_globals2();
    init_worker_thread();
    init_worker_job();
    WorkerPool = class {
      static isSupported() {
        return WorkerThread.isSupported();
      }
      constructor(props) {
        _defineProperty(this, "name", "unnamed");
        _defineProperty(this, "source", void 0);
        _defineProperty(this, "url", void 0);
        _defineProperty(this, "maxConcurrency", 1);
        _defineProperty(this, "maxMobileConcurrency", 1);
        _defineProperty(this, "onDebug", () => {
        });
        _defineProperty(this, "reuseWorkers", true);
        _defineProperty(this, "props", {});
        _defineProperty(this, "jobQueue", []);
        _defineProperty(this, "idleQueue", []);
        _defineProperty(this, "count", 0);
        _defineProperty(this, "isDestroyed", false);
        this.source = props.source;
        this.url = props.url;
        this.setProps(props);
      }
      destroy() {
        this.idleQueue.forEach((worker) => worker.destroy());
        this.isDestroyed = true;
      }
      setProps(props) {
        this.props = {
          ...this.props,
          ...props
        };
        if (props.name !== void 0) {
          this.name = props.name;
        }
        if (props.maxConcurrency !== void 0) {
          this.maxConcurrency = props.maxConcurrency;
        }
        if (props.maxMobileConcurrency !== void 0) {
          this.maxMobileConcurrency = props.maxMobileConcurrency;
        }
        if (props.reuseWorkers !== void 0) {
          this.reuseWorkers = props.reuseWorkers;
        }
        if (props.onDebug !== void 0) {
          this.onDebug = props.onDebug;
        }
      }
      async startJob(name) {
        let onMessage = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : (job, type, data) => job.done(data);
        let onError = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : (job, error) => job.error(error);
        const startPromise = new Promise((onStart) => {
          this.jobQueue.push({
            name,
            onMessage,
            onError,
            onStart
          });
          return this;
        });
        this._startQueuedJob();
        return await startPromise;
      }
      async _startQueuedJob() {
        if (!this.jobQueue.length) {
          return;
        }
        const workerThread = this._getAvailableWorker();
        if (!workerThread) {
          return;
        }
        const queuedJob = this.jobQueue.shift();
        if (queuedJob) {
          this.onDebug({
            message: "Starting job",
            name: queuedJob.name,
            workerThread,
            backlog: this.jobQueue.length
          });
          const job = new WorkerJob(queuedJob.name, workerThread);
          workerThread.onMessage = (data) => queuedJob.onMessage(job, data.type, data.payload);
          workerThread.onError = (error) => queuedJob.onError(job, error);
          queuedJob.onStart(job);
          try {
            await job.result;
          } finally {
            this.returnWorkerToQueue(workerThread);
          }
        }
      }
      returnWorkerToQueue(worker) {
        const shouldDestroyWorker = this.isDestroyed || !this.reuseWorkers || this.count > this._getMaxConcurrency();
        if (shouldDestroyWorker) {
          worker.destroy();
          this.count--;
        } else {
          this.idleQueue.push(worker);
        }
        if (!this.isDestroyed) {
          this._startQueuedJob();
        }
      }
      _getAvailableWorker() {
        if (this.idleQueue.length > 0) {
          return this.idleQueue.shift() || null;
        }
        if (this.count < this._getMaxConcurrency()) {
          this.count++;
          const name = "".concat(this.name.toLowerCase(), " (#").concat(this.count, " of ").concat(this.maxConcurrency, ")");
          return new WorkerThread({
            name,
            source: this.source,
            url: this.url
          });
        }
        return null;
      }
      _getMaxConcurrency() {
        return isMobile ? this.maxMobileConcurrency : this.maxConcurrency;
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-farm.js
var DEFAULT_PROPS, WorkerFarm;
var init_worker_farm = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-farm.js"() {
    init_defineProperty();
    init_worker_pool();
    init_worker_thread();
    DEFAULT_PROPS = {
      maxConcurrency: 3,
      maxMobileConcurrency: 1,
      reuseWorkers: true,
      onDebug: () => {
      }
    };
    WorkerFarm = class _WorkerFarm {
      static isSupported() {
        return WorkerThread.isSupported();
      }
      static getWorkerFarm() {
        let props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
        _WorkerFarm._workerFarm = _WorkerFarm._workerFarm || new _WorkerFarm({});
        _WorkerFarm._workerFarm.setProps(props);
        return _WorkerFarm._workerFarm;
      }
      constructor(props) {
        _defineProperty(this, "props", void 0);
        _defineProperty(this, "workerPools", /* @__PURE__ */ new Map());
        this.props = {
          ...DEFAULT_PROPS
        };
        this.setProps(props);
        this.workerPools = /* @__PURE__ */ new Map();
      }
      destroy() {
        for (const workerPool of this.workerPools.values()) {
          workerPool.destroy();
        }
        this.workerPools = /* @__PURE__ */ new Map();
      }
      setProps(props) {
        this.props = {
          ...this.props,
          ...props
        };
        for (const workerPool of this.workerPools.values()) {
          workerPool.setProps(this._getWorkerPoolProps());
        }
      }
      getWorkerPool(options) {
        const {
          name,
          source,
          url
        } = options;
        let workerPool = this.workerPools.get(name);
        if (!workerPool) {
          workerPool = new WorkerPool({
            name,
            source,
            url
          });
          workerPool.setProps(this._getWorkerPoolProps());
          this.workerPools.set(name, workerPool);
        }
        return workerPool;
      }
      _getWorkerPoolProps() {
        return {
          maxConcurrency: this.props.maxConcurrency,
          maxMobileConcurrency: this.props.maxMobileConcurrency,
          reuseWorkers: this.props.reuseWorkers,
          onDebug: this.props.onDebug
        };
      }
    };
    _defineProperty(WorkerFarm, "_workerFarm", void 0);
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-body.js
function getParentPort() {
  let parentPort;
  try {
    eval("globalThis.parentPort = require('worker_threads').parentPort");
    parentPort = globalThis.parentPort;
  } catch {
  }
  return parentPort;
}
function isKnownMessage(message) {
  const {
    type,
    data
  } = message;
  return type === "message" && data && typeof data.source === "string" && data.source.startsWith("loaders.gl");
}
var onMessageWrapperMap, WorkerBody;
var init_worker_body = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-farm/worker-body.js"() {
    init_get_transfer_list();
    onMessageWrapperMap = /* @__PURE__ */ new Map();
    WorkerBody = class {
      static inWorkerThread() {
        return typeof self !== "undefined" || Boolean(getParentPort());
      }
      static set onmessage(onMessage) {
        function handleMessage(message) {
          const parentPort3 = getParentPort();
          const {
            type,
            payload
          } = parentPort3 ? message : message.data;
          onMessage(type, payload);
        }
        const parentPort2 = getParentPort();
        if (parentPort2) {
          parentPort2.on("message", handleMessage);
          parentPort2.on("exit", () => console.debug("Node worker closing"));
        } else {
          globalThis.onmessage = handleMessage;
        }
      }
      static addEventListener(onMessage) {
        let onMessageWrapper = onMessageWrapperMap.get(onMessage);
        if (!onMessageWrapper) {
          onMessageWrapper = (message) => {
            if (!isKnownMessage(message)) {
              return;
            }
            const parentPort3 = getParentPort();
            const {
              type,
              payload
            } = parentPort3 ? message : message.data;
            onMessage(type, payload);
          };
        }
        const parentPort2 = getParentPort();
        if (parentPort2) {
          console.error("not implemented");
        } else {
          globalThis.addEventListener("message", onMessageWrapper);
        }
      }
      static removeEventListener(onMessage) {
        const onMessageWrapper = onMessageWrapperMap.get(onMessage);
        onMessageWrapperMap.delete(onMessage);
        const parentPort2 = getParentPort();
        if (parentPort2) {
          console.error("not implemented");
        } else {
          globalThis.removeEventListener("message", onMessageWrapper);
        }
      }
      static postMessage(type, payload) {
        const data = {
          source: "loaders.gl",
          type,
          payload
        };
        const transferList = getTransferList(payload);
        const parentPort2 = getParentPort();
        if (parentPort2) {
          parentPort2.postMessage(data, transferList);
        } else {
          globalThis.postMessage(data, transferList);
        }
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/get-worker-url.js
var init_get_worker_url = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/get-worker-url.js"() {
    init_assert2();
    init_version2();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/process-on-worker.js
var init_process_on_worker = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/process-on-worker.js"() {
    init_worker_farm();
    init_get_worker_url();
    init_get_transfer_list();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/async-queue/async-queue.js
var _Symbol$asyncIterator, AsyncQueue;
var init_async_queue = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/async-queue/async-queue.js"() {
    init_defineProperty();
    _Symbol$asyncIterator = Symbol.asyncIterator;
    AsyncQueue = class {
      constructor() {
        _defineProperty(this, "_values", void 0);
        _defineProperty(this, "_settlers", void 0);
        _defineProperty(this, "_closed", void 0);
        this._values = [];
        this._settlers = [];
        this._closed = false;
      }
      [_Symbol$asyncIterator]() {
        return this;
      }
      push(value) {
        return this.enqueue(value);
      }
      enqueue(value) {
        if (this._closed) {
          throw new Error("Closed");
        }
        if (this._settlers.length > 0) {
          if (this._values.length > 0) {
            throw new Error("Illegal internal state");
          }
          const settler = this._settlers.shift();
          if (value instanceof Error) {
            settler.reject(value);
          } else {
            settler.resolve({
              value
            });
          }
        } else {
          this._values.push(value);
        }
      }
      close() {
        while (this._settlers.length > 0) {
          const settler = this._settlers.shift();
          settler.resolve({
            done: true
          });
        }
        this._closed = true;
      }
      next() {
        if (this._values.length > 0) {
          const value = this._values.shift();
          if (value instanceof Error) {
            return Promise.reject(value);
          }
          return Promise.resolve({
            done: false,
            value
          });
        }
        if (this._closed) {
          if (this._settlers.length > 0) {
            throw new Error("Illegal internal state");
          }
          return Promise.resolve({
            done: true,
            value: void 0
          });
        }
        return new Promise((resolve, reject) => {
          this._settlers.push({
            resolve,
            reject
          });
        });
      }
    };
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/create-worker.js
var init_create_worker = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/create-worker.js"() {
    init_async_queue();
    init_worker_body();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/validate-worker-version.js
var init_validate_worker_version = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/worker-api/validate-worker-version.js"() {
    init_assert2();
    init_version2();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/node/require-utils.browser.js
var init_require_utils_browser = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/node/require-utils.browser.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/library-utils/library-utils.js
var init_library_utils = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/library-utils/library-utils.js"() {
    init_globals2();
    init_require_utils_browser();
    init_assert2();
    init_version2();
  }
});

// (disabled):../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/process-utils/child-process-proxy
var require_child_process_proxy = __commonJS({
  "(disabled):../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/lib/process-utils/child-process-proxy"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/index.js
var import_child_process_proxy;
var init_esm2 = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/worker-utils/dist/esm/index.js"() {
    init_version2();
    init_assert2();
    init_globals2();
    init_worker_job();
    init_worker_thread();
    init_worker_farm();
    init_worker_pool();
    init_worker_body();
    init_process_on_worker();
    init_create_worker();
    init_get_worker_url();
    init_validate_worker_version();
    init_get_transfer_list();
    init_library_utils();
    init_async_queue();
    import_child_process_proxy = __toESM(require_child_process_proxy());
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/create-loader-worker.js
var init_create_loader_worker = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/create-loader-worker.js"() {
    init_esm2();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/parse-with-worker.js
var init_parse_with_worker = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/parse-with-worker.js"() {
    init_esm2();
    init_esm2();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/encode-with-worker.js
var init_encode_with_worker = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/worker-loader-utils/encode-with-worker.js"() {
    init_esm2();
    init_globals();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/get-first-characters.js
var init_get_first_characters = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/get-first-characters.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/parser-utils/parse-json.js
var init_parse_json = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/parser-utils/parse-json.js"() {
    init_get_first_characters();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/array-buffer-utils.js
var init_array_buffer_utils = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/array-buffer-utils.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/memory-copy-utils.js
var init_memory_copy_utils = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/memory-copy-utils.js"() {
    init_assert();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/dataview-copy-utils.js
var init_dataview_copy_utils = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/dataview-copy-utils.js"() {
    init_memory_copy_utils();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/iterators/text-iterators.js
var init_text_iterators = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/iterators/text-iterators.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/iterators/async-iteration.js
var init_async_iteration = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/iterators/async-iteration.js"() {
    init_array_buffer_utils();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/request-utils/request-scheduler.js
var init_request_scheduler = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/request-utils/request-scheduler.js"() {
    init_defineProperty();
    init_esm();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/file-aliases.js
function resolvePath(filename) {
  for (const alias in fileAliases) {
    if (filename.startsWith(alias)) {
      const replacement = fileAliases[alias];
      filename = filename.replace(alias, replacement);
    }
  }
  if (!filename.startsWith("http://") && !filename.startsWith("https://")) {
    filename = "".concat(pathPrefix).concat(filename);
  }
  return filename;
}
var pathPrefix, fileAliases;
var init_file_aliases = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/file-aliases.js"() {
    pathPrefix = "";
    fileAliases = {};
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/json-loader.js
var init_json_loader = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/json-loader.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/buffer.browser.js
var init_buffer_browser = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/buffer.browser.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/memory-conversion-utils.js
var init_memory_conversion_utils = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/binary-utils/memory-conversion-utils.js"() {
    init_buffer_browser();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/promisify.js
var init_promisify = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/promisify.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/get-cwd.js
var init_get_cwd = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/get-cwd.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/path.js
var init_path = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/path-utils/path.js"() {
    init_get_cwd();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/fs.browser.js
var init_fs_browser = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/fs.browser.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/stream.browser.js
var init_stream_browser = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/node/stream.browser.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/readable-file.js
var init_readable_file = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/readable-file.js"() {
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/writable-file.js
var init_writable_file = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/writable-file.js"() {
    init_globals();
    init_fs_browser();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/node-filesystem.js
var init_node_filesystem = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/lib/filesystems/node-filesystem.js"() {
    init_fs_browser();
  }
});

// ../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/index.js
var init_esm3 = __esm({
  "../node_modules/@loaders.gl/images/node_modules/@loaders.gl/loader-utils/dist/esm/index.js"() {
    init_assert();
    init_globals();
    init_merge_loader_options();
    init_create_loader_worker();
    init_parse_with_worker();
    init_encode_with_worker();
    init_parse_json();
    init_array_buffer_utils();
    init_memory_copy_utils();
    init_dataview_copy_utils();
    init_get_first_characters();
    init_text_iterators();
    init_async_iteration();
    init_request_scheduler();
    init_file_aliases();
    init_file_aliases();
    init_json_loader();
    init_memory_conversion_utils();
    init_promisify();
    init_path();
    init_fs_browser();
    init_stream_browser();
    init_readable_file();
    init_writable_file();
    init_node_filesystem();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/category-api/image-type.js
function isImageTypeSupported(type) {
  switch (type) {
    case "auto":
      return IMAGE_BITMAP_SUPPORTED || IMAGE_SUPPORTED || DATA_SUPPORTED;
    case "imagebitmap":
      return IMAGE_BITMAP_SUPPORTED;
    case "image":
      return IMAGE_SUPPORTED;
    case "data":
      return DATA_SUPPORTED;
    default:
      throw new Error("@loaders.gl/images: image ".concat(type, " not supported in this environment"));
  }
}
function getDefaultImageType() {
  if (IMAGE_BITMAP_SUPPORTED) {
    return "imagebitmap";
  }
  if (IMAGE_SUPPORTED) {
    return "image";
  }
  if (DATA_SUPPORTED) {
    return "data";
  }
  throw new Error("Install '@loaders.gl/polyfills' to parse images under Node.js");
}
var _parseImageNode, IMAGE_SUPPORTED, IMAGE_BITMAP_SUPPORTED, NODE_IMAGE_SUPPORTED, DATA_SUPPORTED;
var init_image_type = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/category-api/image-type.js"() {
    init_esm3();
    ({
      _parseImageNode
    } = globalThis);
    IMAGE_SUPPORTED = typeof Image !== "undefined";
    IMAGE_BITMAP_SUPPORTED = typeof ImageBitmap !== "undefined";
    NODE_IMAGE_SUPPORTED = Boolean(_parseImageNode);
    DATA_SUPPORTED = isBrowser ? true : NODE_IMAGE_SUPPORTED;
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/category-api/parsed-image-api.js
function isImage(image) {
  return Boolean(getImageTypeOrNull(image));
}
function getImageType(image) {
  const format = getImageTypeOrNull(image);
  if (!format) {
    throw new Error("Not an image");
  }
  return format;
}
function getImageSize(image) {
  return getImageData(image);
}
function getImageData(image) {
  switch (getImageType(image)) {
    case "data":
      return image;
    case "image":
    case "imagebitmap":
      const canvas = document.createElement("canvas");
      const context = canvas.getContext("2d");
      if (!context) {
        throw new Error("getImageData");
      }
      canvas.width = image.width;
      canvas.height = image.height;
      context.drawImage(image, 0, 0);
      return context.getImageData(0, 0, image.width, image.height);
    default:
      throw new Error("getImageData");
  }
}
function getImageTypeOrNull(image) {
  if (typeof ImageBitmap !== "undefined" && image instanceof ImageBitmap) {
    return "imagebitmap";
  }
  if (typeof Image !== "undefined" && image instanceof Image) {
    return "image";
  }
  if (image && typeof image === "object" && image.data && image.width && image.height) {
    return "data";
  }
  return null;
}
var init_parsed_image_api = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/category-api/parsed-image-api.js"() {
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/parsers/svg-utils.js
function isSVG(url) {
  return url && (SVG_DATA_URL_PATTERN.test(url) || SVG_URL_PATTERN.test(url));
}
function getBlobOrSVGDataUrl(arrayBuffer, url) {
  if (isSVG(url)) {
    const textDecoder = new TextDecoder();
    let xmlText = textDecoder.decode(arrayBuffer);
    try {
      if (typeof unescape === "function" && typeof encodeURIComponent === "function") {
        xmlText = unescape(encodeURIComponent(xmlText));
      }
    } catch (error) {
      throw new Error(error.message);
    }
    const src = "data:image/svg+xml;base64,".concat(btoa(xmlText));
    return src;
  }
  return getBlob(arrayBuffer, url);
}
function getBlob(arrayBuffer, url) {
  if (isSVG(url)) {
    throw new Error("SVG cannot be parsed directly to imagebitmap");
  }
  return new Blob([new Uint8Array(arrayBuffer)]);
}
var SVG_DATA_URL_PATTERN, SVG_URL_PATTERN;
var init_svg_utils = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/parsers/svg-utils.js"() {
    SVG_DATA_URL_PATTERN = /^data:image\/svg\+xml/;
    SVG_URL_PATTERN = /\.svg((\?|#).*)?$/;
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-image.js
async function parseToImage(arrayBuffer, options, url) {
  const blobOrDataUrl = getBlobOrSVGDataUrl(arrayBuffer, url);
  const URL2 = self.URL || self.webkitURL;
  const objectUrl = typeof blobOrDataUrl !== "string" && URL2.createObjectURL(blobOrDataUrl);
  try {
    return await loadToImage(objectUrl || blobOrDataUrl, options);
  } finally {
    if (objectUrl) {
      URL2.revokeObjectURL(objectUrl);
    }
  }
}
async function loadToImage(url, options) {
  const image = new Image();
  image.src = url;
  if (options.image && options.image.decode && image.decode) {
    await image.decode();
    return image;
  }
  return await new Promise((resolve, reject) => {
    try {
      image.onload = () => resolve(image);
      image.onerror = (err) => reject(new Error("Could not load image ".concat(url, ": ").concat(err)));
    } catch (error) {
      reject(error);
    }
  });
}
var init_parse_to_image = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-image.js"() {
    init_svg_utils();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-image-bitmap.js
async function parseToImageBitmap(arrayBuffer, options, url) {
  let blob;
  if (isSVG(url)) {
    const image = await parseToImage(arrayBuffer, options, url);
    blob = image;
  } else {
    blob = getBlob(arrayBuffer, url);
  }
  const imagebitmapOptions = options && options.imagebitmap;
  return await safeCreateImageBitmap(blob, imagebitmapOptions);
}
async function safeCreateImageBitmap(blob) {
  let imagebitmapOptions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : null;
  if (isEmptyObject(imagebitmapOptions) || !imagebitmapOptionsSupported) {
    imagebitmapOptions = null;
  }
  if (imagebitmapOptions) {
    try {
      return await createImageBitmap(blob, imagebitmapOptions);
    } catch (error) {
      console.warn(error);
      imagebitmapOptionsSupported = false;
    }
  }
  return await createImageBitmap(blob);
}
function isEmptyObject(object) {
  for (const key in object || EMPTY_OBJECT) {
    return false;
  }
  return true;
}
var EMPTY_OBJECT, imagebitmapOptionsSupported;
var init_parse_to_image_bitmap = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-image-bitmap.js"() {
    init_svg_utils();
    init_parse_to_image();
    EMPTY_OBJECT = {};
    imagebitmapOptionsSupported = true;
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/category-api/parse-isobmff-binary.js
function getISOBMFFMediaType(buffer) {
  if (!checkString(buffer, "ftyp", 4)) {
    return null;
  }
  if ((buffer[8] & 96) === 0) {
    return null;
  }
  return decodeMajorBrand(buffer);
}
function decodeMajorBrand(buffer) {
  const brandMajor = getUTF8String(buffer, 8, 12).replace("\0", " ").trim();
  switch (brandMajor) {
    case "avif":
    case "avis":
      return {
        extension: "avif",
        mimeType: "image/avif"
      };
    default:
      return null;
  }
}
function getUTF8String(array, start, end) {
  return String.fromCharCode(...array.slice(start, end));
}
function stringToBytes(string) {
  return [...string].map((character) => character.charCodeAt(0));
}
function checkString(buffer, header) {
  let offset = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
  const headerBytes = stringToBytes(header);
  for (let i = 0; i < headerBytes.length; ++i) {
    if (headerBytes[i] !== buffer[i + offset]) {
      return false;
    }
  }
  return true;
}
var init_parse_isobmff_binary = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/category-api/parse-isobmff-binary.js"() {
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/category-api/binary-image-api.js
function getBinaryImageMetadata(binaryData) {
  const dataView = toDataView(binaryData);
  return getPngMetadata(dataView) || getJpegMetadata(dataView) || getGifMetadata(dataView) || getBmpMetadata(dataView) || getISOBMFFMetadata(dataView);
}
function getISOBMFFMetadata(binaryData) {
  const buffer = new Uint8Array(binaryData instanceof DataView ? binaryData.buffer : binaryData);
  const mediaType = getISOBMFFMediaType(buffer);
  if (!mediaType) {
    return null;
  }
  return {
    mimeType: mediaType.mimeType,
    width: 0,
    height: 0
  };
}
function getPngMetadata(binaryData) {
  const dataView = toDataView(binaryData);
  const isPng = dataView.byteLength >= 24 && dataView.getUint32(0, BIG_ENDIAN) === 2303741511;
  if (!isPng) {
    return null;
  }
  return {
    mimeType: "image/png",
    width: dataView.getUint32(16, BIG_ENDIAN),
    height: dataView.getUint32(20, BIG_ENDIAN)
  };
}
function getGifMetadata(binaryData) {
  const dataView = toDataView(binaryData);
  const isGif = dataView.byteLength >= 10 && dataView.getUint32(0, BIG_ENDIAN) === 1195984440;
  if (!isGif) {
    return null;
  }
  return {
    mimeType: "image/gif",
    width: dataView.getUint16(6, LITTLE_ENDIAN),
    height: dataView.getUint16(8, LITTLE_ENDIAN)
  };
}
function getBmpMetadata(binaryData) {
  const dataView = toDataView(binaryData);
  const isBmp = dataView.byteLength >= 14 && dataView.getUint16(0, BIG_ENDIAN) === 16973 && dataView.getUint32(2, LITTLE_ENDIAN) === dataView.byteLength;
  if (!isBmp) {
    return null;
  }
  return {
    mimeType: "image/bmp",
    width: dataView.getUint32(18, LITTLE_ENDIAN),
    height: dataView.getUint32(22, LITTLE_ENDIAN)
  };
}
function getJpegMetadata(binaryData) {
  const dataView = toDataView(binaryData);
  const isJpeg = dataView.byteLength >= 3 && dataView.getUint16(0, BIG_ENDIAN) === 65496 && dataView.getUint8(2) === 255;
  if (!isJpeg) {
    return null;
  }
  const {
    tableMarkers,
    sofMarkers
  } = getJpegMarkers();
  let i = 2;
  while (i + 9 < dataView.byteLength) {
    const marker = dataView.getUint16(i, BIG_ENDIAN);
    if (sofMarkers.has(marker)) {
      return {
        mimeType: "image/jpeg",
        height: dataView.getUint16(i + 5, BIG_ENDIAN),
        width: dataView.getUint16(i + 7, BIG_ENDIAN)
      };
    }
    if (!tableMarkers.has(marker)) {
      return null;
    }
    i += 2;
    i += dataView.getUint16(i, BIG_ENDIAN);
  }
  return null;
}
function getJpegMarkers() {
  const tableMarkers = /* @__PURE__ */ new Set([65499, 65476, 65484, 65501, 65534]);
  for (let i = 65504; i < 65520; ++i) {
    tableMarkers.add(i);
  }
  const sofMarkers = /* @__PURE__ */ new Set([65472, 65473, 65474, 65475, 65477, 65478, 65479, 65481, 65482, 65483, 65485, 65486, 65487, 65502]);
  return {
    tableMarkers,
    sofMarkers
  };
}
function toDataView(data) {
  if (data instanceof DataView) {
    return data;
  }
  if (ArrayBuffer.isView(data)) {
    return new DataView(data.buffer);
  }
  if (data instanceof ArrayBuffer) {
    return new DataView(data);
  }
  throw new Error("toDataView");
}
var BIG_ENDIAN, LITTLE_ENDIAN;
var init_binary_image_api = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/category-api/binary-image-api.js"() {
    init_parse_isobmff_binary();
    BIG_ENDIAN = false;
    LITTLE_ENDIAN = true;
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-node-image.js
async function parseToNodeImage(arrayBuffer, options) {
  const {
    mimeType
  } = getBinaryImageMetadata(arrayBuffer) || {};
  const _parseImageNode2 = globalThis._parseImageNode;
  assert(_parseImageNode2);
  return await _parseImageNode2(arrayBuffer, mimeType);
}
var init_parse_to_node_image = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-to-node-image.js"() {
    init_esm3();
    init_binary_image_api();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-image.js
async function parseImage(arrayBuffer, options, context) {
  options = options || {};
  const imageOptions = options.image || {};
  const imageType = imageOptions.type || "auto";
  const {
    url
  } = context || {};
  const loadType = getLoadableImageType(imageType);
  let image;
  switch (loadType) {
    case "imagebitmap":
      image = await parseToImageBitmap(arrayBuffer, options, url);
      break;
    case "image":
      image = await parseToImage(arrayBuffer, options, url);
      break;
    case "data":
      image = await parseToNodeImage(arrayBuffer, options);
      break;
    default:
      assert(false);
  }
  if (imageType === "data") {
    image = getImageData(image);
  }
  return image;
}
function getLoadableImageType(type) {
  switch (type) {
    case "auto":
    case "data":
      return getDefaultImageType();
    default:
      isImageTypeSupported(type);
      return type;
  }
}
var init_parse_image = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/parsers/parse-image.js"() {
    init_esm3();
    init_image_type();
    init_parsed_image_api();
    init_parse_to_image();
    init_parse_to_image_bitmap();
    init_parse_to_node_image();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/image-loader.js
var EXTENSIONS, MIME_TYPES, DEFAULT_IMAGE_LOADER_OPTIONS, ImageLoader;
var init_image_loader = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/image-loader.js"() {
    init_version();
    init_parse_image();
    init_binary_image_api();
    EXTENSIONS = ["png", "jpg", "jpeg", "gif", "webp", "bmp", "ico", "svg", "avif"];
    MIME_TYPES = ["image/png", "image/jpeg", "image/gif", "image/webp", "image/avif", "image/bmp", "image/vnd.microsoft.icon", "image/svg+xml"];
    DEFAULT_IMAGE_LOADER_OPTIONS = {
      image: {
        type: "auto",
        decode: true
      }
    };
    ImageLoader = {
      id: "image",
      module: "images",
      name: "Images",
      version: VERSION,
      mimeTypes: MIME_TYPES,
      extensions: EXTENSIONS,
      parse: parseImage,
      tests: [(arrayBuffer) => Boolean(getBinaryImageMetadata(new DataView(arrayBuffer)))],
      options: DEFAULT_IMAGE_LOADER_OPTIONS
    };
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/encoders/encode-image.js
async function encodeImage(image, options) {
  options = options || {};
  options.image = options.image || {};
  return _encodeImageNode ? _encodeImageNode(image, {
    type: options.image.mimeType
  }) : encodeImageInBrowser(image, options);
}
async function encodeImageInBrowser(image, options) {
  const {
    mimeType,
    jpegQuality
  } = options.image;
  const {
    width,
    height
  } = getImageSize(image);
  const canvas = document.createElement("canvas");
  canvas.width = width;
  canvas.height = height;
  drawImageToCanvas(image, canvas);
  const blob = await new Promise((resolve) => {
    if (jpegQuality && qualityParamSupported) {
      try {
        canvas.toBlob(resolve, mimeType, jpegQuality);
        return;
      } catch (error) {
        qualityParamSupported = false;
      }
    }
    canvas.toBlob(resolve, mimeType);
  });
  if (!blob) {
    throw new Error("image encoding failed");
  }
  return await blob.arrayBuffer();
}
function drawImageToCanvas(image, canvas) {
  let x = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
  let y = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 0;
  if (x === 0 && y === 0 && typeof ImageBitmap !== "undefined" && image instanceof ImageBitmap) {
    const context2 = canvas.getContext("bitmaprenderer");
    if (context2) {
      context2.transferFromImageBitmap(image);
      return canvas;
    }
  }
  const context = canvas.getContext("2d");
  if (image.data) {
    const clampedArray = new Uint8ClampedArray(image.data);
    const imageData = new ImageData(clampedArray, image.width, image.height);
    context.putImageData(imageData, 0, 0);
    return canvas;
  }
  context.drawImage(image, 0, 0);
  return canvas;
}
var _encodeImageNode, qualityParamSupported;
var init_encode_image = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/encoders/encode-image.js"() {
    init_parsed_image_api();
    ({
      _encodeImageNode
    } = globalThis);
    qualityParamSupported = true;
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/image-writer.js
var ImageWriter;
var init_image_writer = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/image-writer.js"() {
    init_version();
    init_encode_image();
    ImageWriter = {
      name: "Images",
      id: "image",
      module: "images",
      version: VERSION,
      extensions: ["jpeg"],
      options: {
        image: {
          mimeType: "image/png",
          jpegQuality: null
        }
      },
      encode: encodeImage
    };
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/category-api/image-format.js
async function getSupportedImageFormats() {
  if (mimeTypeSupportedPromise) {
    return await mimeTypeSupportedPromise;
  }
  const supportedMimeTypes = /* @__PURE__ */ new Set();
  for (const mimeType of MIME_TYPES2) {
    const supported = isBrowser ? await checkBrowserImageFormatSupportAsync(mimeType) : checkNodeImageFormatSupport(mimeType);
    if (supported) {
      supportedMimeTypes.add(mimeType);
    }
  }
  return supportedMimeTypes;
}
function isImageFormatSupported(mimeType) {
  if (mimeTypeSupportedSync[mimeType] === void 0) {
    const supported = isBrowser ? checkBrowserImageFormatSupport(mimeType) : checkNodeImageFormatSupport(mimeType);
    mimeTypeSupportedSync[mimeType] = supported;
  }
  return mimeTypeSupportedSync[mimeType];
}
function checkNodeImageFormatSupport(mimeType) {
  const NODE_FORMAT_SUPPORT = ["image/png", "image/jpeg", "image/gif"];
  const {
    _parseImageNode: _parseImageNode2,
    _imageFormatsNode = NODE_FORMAT_SUPPORT
  } = globalThis;
  return Boolean(_parseImageNode2) && _imageFormatsNode.includes(mimeType);
}
function checkBrowserImageFormatSupport(mimeType) {
  switch (mimeType) {
    case "image/avif":
    case "image/webp":
      return testBrowserImageFormatSupport(mimeType);
    default:
      return true;
  }
}
async function checkBrowserImageFormatSupportAsync(mimeType) {
  const dataURL = TEST_IMAGE[mimeType];
  return dataURL ? await testBrowserImageFormatSupportAsync(dataURL) : true;
}
function testBrowserImageFormatSupport(mimeType) {
  try {
    const element = document.createElement("canvas");
    const dataURL = element.toDataURL(mimeType);
    return dataURL.indexOf("data:".concat(mimeType)) === 0;
  } catch {
    return false;
  }
}
async function testBrowserImageFormatSupportAsync(testImageDataURL) {
  return new Promise((resolve) => {
    const image = new Image();
    image.src = testImageDataURL;
    image.onload = () => resolve(image.height > 0);
    image.onerror = () => resolve(false);
  });
}
var MIME_TYPES2, mimeTypeSupportedPromise, mimeTypeSupportedSync, TEST_IMAGE;
var init_image_format = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/category-api/image-format.js"() {
    init_esm3();
    MIME_TYPES2 = ["image/png", "image/jpeg", "image/gif", "image/webp", "image/avif", "image/tiff", "image/svg", "image/svg+xml", "image/bmp", "image/vnd.microsoft.icon"];
    mimeTypeSupportedPromise = null;
    mimeTypeSupportedSync = {};
    TEST_IMAGE = {
      "image/avif": "data:image/avif;base64,",
      "image/webp": "data:image/webp;base64,UklGRiIAAABXRUJQVlA4IBYAAAAwAQCdASoBAAEADsD+JaQAA3AAAAAA"
    };
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/generate-url.js
function generateUrl(getUrl, options, urlOptions) {
  let url = getUrl;
  if (typeof getUrl === "function") {
    url = getUrl({
      ...options,
      ...urlOptions
    });
  }
  assert(typeof url === "string");
  const {
    baseUrl
  } = options;
  if (baseUrl) {
    url = baseUrl[baseUrl.length - 1] === "/" ? "".concat(baseUrl).concat(url) : "".concat(baseUrl, "/").concat(url);
  }
  return resolvePath(url);
}
var init_generate_url = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/generate-url.js"() {
    init_esm3();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/async-deep-map.js
async function asyncDeepMap(tree, func) {
  let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  return await mapSubtree(tree, func, options);
}
async function mapSubtree(object, func, options) {
  if (Array.isArray(object)) {
    return await mapArray(object, func, options);
  }
  if (isObject(object)) {
    return await mapObject(object, func, options);
  }
  const url = object;
  return await func(url, options);
}
async function mapObject(object, func, options) {
  const promises = [];
  const values = {};
  for (const key in object) {
    const url = object[key];
    const promise = mapSubtree(url, func, options).then((value) => {
      values[key] = value;
    });
    promises.push(promise);
  }
  await Promise.all(promises);
  return values;
}
async function mapArray(urlArray, func) {
  let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  const promises = urlArray.map((url) => mapSubtree(url, func, options));
  return await Promise.all(promises);
}
var isObject;
var init_async_deep_map = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/async-deep-map.js"() {
    isObject = (value) => value && typeof value === "object";
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/deep-load.js
async function deepLoad(urlTree, load, options) {
  return await asyncDeepMap(urlTree, (url) => shallowLoad(url, load, options));
}
async function shallowLoad(url, load, options) {
  const response = await fetch(url, options.fetch);
  const arrayBuffer = await response.arrayBuffer();
  return await load(arrayBuffer, options);
}
var init_deep_load = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/deep-load.js"() {
    init_async_deep_map();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/load-image.js
async function loadImage(getUrl) {
  let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  const imageUrls = await getImageUrls(getUrl, options);
  return await deepLoad(imageUrls, parseImage, options);
}
async function getImageUrls(getUrl, options) {
  let urlOptions = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  const mipLevels = options && options.image && options.image.mipLevels || 0;
  return mipLevels !== 0 ? await getMipmappedImageUrls(getUrl, mipLevels, options, urlOptions) : generateUrl(getUrl, options, urlOptions);
}
async function getMipmappedImageUrls(getUrl, mipLevels, options, urlOptions) {
  const urls = [];
  if (mipLevels === "auto") {
    const url = generateUrl(getUrl, options, {
      ...urlOptions,
      lod: 0
    });
    const image = await shallowLoad(url, parseImage, options);
    const {
      width,
      height
    } = getImageSize(image);
    mipLevels = getMipLevels({
      width,
      height
    });
    urls.push(url);
  }
  assert(mipLevels > 0);
  for (let mipLevel = urls.length; mipLevel < mipLevels; ++mipLevel) {
    const url = generateUrl(getUrl, options, {
      ...urlOptions,
      lod: mipLevel
    });
    urls.push(url);
  }
  return urls;
}
function getMipLevels(_ref) {
  let {
    width,
    height
  } = _ref;
  return 1 + Math.floor(Math.log2(Math.max(width, height)));
}
var init_load_image = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/lib/texture-api/load-image.js"() {
    init_esm3();
    init_parse_image();
    init_parsed_image_api();
    init_generate_url();
    init_deep_load();
  }
});

// ../node_modules/@loaders.gl/images/dist/esm/index.js
var esm_exports = {};
__export(esm_exports, {
  ImageLoader: () => ImageLoader,
  ImageWriter: () => ImageWriter,
  getBinaryImageMetadata: () => getBinaryImageMetadata,
  getDefaultImageType: () => getDefaultImageType,
  getImageData: () => getImageData,
  getImageSize: () => getImageSize,
  getImageType: () => getImageType,
  getSupportedImageFormats: () => getSupportedImageFormats,
  isImage: () => isImage,
  isImageFormatSupported: () => isImageFormatSupported,
  isImageTypeSupported: () => isImageTypeSupported,
  loadImage: () => loadImage
});
var init_esm4 = __esm({
  "../node_modules/@loaders.gl/images/dist/esm/index.js"() {
    init_image_loader();
    init_image_writer();
    init_binary_image_api();
    init_image_type();
    init_parsed_image_api();
    init_image_format();
    init_image_format();
    init_load_image();
  }
});

export {
  getBinaryImageMetadata,
  ImageLoader,
  isImageFormatSupported,
  esm_exports,
  init_esm4 as init_esm
};
//# sourceMappingURL=chunk-QIVSOVQZ.js.map
