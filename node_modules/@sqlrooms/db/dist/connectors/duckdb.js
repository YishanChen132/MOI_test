export const CORE_DUCKDB_CONNECTION_ID = 'duckdb-core';
export function createCoreDuckDbConnection(overrides) {
    return {
        id: CORE_DUCKDB_CONNECTION_ID,
        engineId: 'duckdb',
        title: 'Core DuckDB',
        runtimeSupport: 'both',
        requiresBridge: false,
        isCore: true,
        ...overrides,
    };
}
export function isCoreDuckDbConnection(connectionId) {
    return connectionId === CORE_DUCKDB_CONNECTION_ID;
}
//# sourceMappingURL=duckdb.js.map