import { jsx as _jsx } from "react/jsx-runtime";
import { MAIN_VIEW } from '@sqlrooms/layout-config';
import { cn } from '@sqlrooms/ui';
import { MosaicWindow } from 'react-mosaic-component';
const ENABLE_LAYOUT_REARRANGE = false;
const MosaicTile = (props) => {
    const { id, content, path, isDragging, className } = props;
    const body = (_jsx("div", { className: cn('h-full flex-1 flex-col overflow-hidden p-2', className, isDragging ? 'pointer-events-none' : ''), children: content }));
    if (!ENABLE_LAYOUT_REARRANGE || id === MAIN_VIEW) {
        return body;
    }
    return (_jsx(MosaicWindow, { title: id, 
        // additionalControls={
        // }
        // createNode={() => genRandomStr(6)}
        path: path, 
        // onDragStart={() => console.log('MosaicWindow.onDragStart')}
        // onDragEnd={(type) => console.log('MosaicWindow.onDragEnd', type)}
        renderToolbar: () => id === MAIN_VIEW ? (_jsx("div", {})) : (_jsx("div", { style: { display: 'flex', width: '100%', height: '100%' } })), children: body }));
};
export default MosaicTile;
//# sourceMappingURL=MosaicTile.js.map